<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"
    />

    <title><%= title %></title>
  </head>
  <body>
    <%- include('./partials/user-header') %> <% if (created) {%>
      <div class="alert alert-success">Note has been created!</div>
      <% } %> <% if (error) {%>
      <div class="alert alert-danger">Something went wrong!</div>
      <% } %>

    <div class="container mt-4">
      <div class="row justify-content-center">
        <div class="col-md-4">
          <div class="card text-center ">
            <a class="nav-link" href="/requestsToDoctor">Проверить записи к врачу</a>

          </div>
          <div class="card">
            <div class="card-body">
              <h2 class="card-title text-center">Запись к врачу</h2>
              <hr />
              <h4 class="card-subtitle mb-4 text-center">Заполните форму ниже</h4>
              <form action="/appointment" method="POST">
                <input
                  class="form-control mb-3"
                  type="text"
                  name="name"
                  placeholder="ФИО..."
                />
                <input
                  class="form-control mb-3"
                  type="text"
                  name="phone"
                  placeholder="Номер Телефона..."
                />
                <textarea
                  class="form-control mb-3"
                  name="symptom"
                  placeholder="Опишите вашу проблему..."
                  rows="4"
                ></textarea>
                <button class="btn btn-primary btn-block" type="submit">
                  Записаться
                </button>
              </form>
            </div>
          </div>
        </div>
      </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
  </body>
</html>
